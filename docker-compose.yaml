services:
  moviepicker:
    build: .
    container_name: moviepicker
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - kortregt

networks:
  kortregt:
    external: true
